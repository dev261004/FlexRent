"use client";

import { useEffect, useState } from "react";
import { CheckCircle2, RotateCcw, Truck } from "lucide-react";
import { confirmOrder, getOrders, markOrderPickedUp, markOrderReturned, type RentalOrder } from "@/features/customer/api";
import { Panel } from "@/components/admin/Panel";

const statusStyle: Record<string, string> = { QUOTATION: "bg-blue-500/15 text-blue-700 dark:text-blue-300", CONFIRMED: "bg-blue-500/15 text-blue-700 dark:text-blue-300", PICKED_UP: "bg-accent/15 text-yellow-800 dark:text-accent", RETURNED: "bg-green-500/15 text-green-700 dark:text-green-300", CANCELLED: "bg-black/5 text-chalk dark:bg-white/10" };
const label = (status: string) => status.replaceAll("_", " ").replace(/\b\w/g, char => char.toUpperCase());
export function OperationsTable() {
  const [orders, setOrders] = useState<RentalOrder[]>([]); const [loading, setLoading] = useState(true); const [busy, setBusy] = useState<string | null>(null); const [error, setError] = useState("");
  const load = () => { setLoading(true); getOrders().then(d => setOrders(d.rentalOrders ?? d.orders ?? [])).catch(() => setError("Could not load rental orders.")).finally(() => setLoading(false)); };
  useEffect(load, []);
  const action = async (order: RentalOrder, type: "confirm" | "pickup" | "return") => { setBusy(order.id); setError(""); try { const updated = type === "confirm" ? await confirmOrder(order.id) : type === "pickup" ? await markOrderPickedUp(order.id) : await markOrderReturned(order.id); setOrders(prev => prev.map(item => item.id === order.id ? updated : item)); } catch { setError(`Unable to ${type === "confirm" ? "confirm" : `mark as ${type === "pickup" ? "picked up" : "returned"}`} this order.`); } finally { setBusy(null); } };
  const actionable = orders.filter(o => ["QUOTATION", "CONFIRMED", "PICKED_UP"].includes(o.status));
  return <Panel className="overflow-hidden"><div className="border-b border-border px-5 py-5"><h2 className="font-display text-lg font-bold text-text">Live rental operations</h2><p className="mt-1 text-sm text-chalk">Confirm bookings to collect the security deposit, then track pickup and return.</p></div>{error && <p className="mx-5 mt-4 rounded-xl bg-danger/10 p-3 text-sm text-red-600 dark:text-red-300">{error}</p>}<div className="overflow-x-auto"><table className="w-full min-w-[780px] text-sm"><thead><tr className="border-b border-border bg-black/[.02] text-left text-xs uppercase tracking-wide text-chalk dark:bg-white/[.02]"><th className="px-5 py-3">Order</th><th className="px-5 py-3">Customer</th><th className="px-5 py-3">Product</th><th className="px-5 py-3">Rental period</th><th className="px-5 py-3">Status</th><th className="px-5 py-3">Action</th></tr></thead><tbody>{loading ? <tr><td colSpan={6} className="p-10 text-center text-chalk">Loading orders…</td></tr> : actionable.length === 0 ? <tr><td colSpan={6} className="p-10 text-center text-chalk">No active customer bookings yet.</td></tr> : actionable.map(order => <tr key={order.id} className="border-t border-border/60"><td className="px-5 py-4 font-mono text-xs font-bold text-text">{order.rentalNumber}</td><td className="px-5 py-4 text-text">{order.customer?.fullName ?? ([order.customer?.firstName, order.customer?.lastName].filter(Boolean).join(" ") || "Customer")}</td><td className="px-5 py-4 text-text">{order.items.map(item => item.product.name).join(", ")}</td><td className="px-5 py-4 text-chalk">{new Date(order.rentalStart).toLocaleDateString("en-IN", { day: "numeric", month: "short" })} – {new Date(order.rentalEnd).toLocaleDateString("en-IN", { day: "numeric", month: "short" })}</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${statusStyle[order.status] ?? statusStyle.QUOTATION}`}>{label(order.status)}</span></td><td className="px-5 py-4">{order.status === "QUOTATION" ? <button disabled={busy === order.id} onClick={() => action(order, "confirm")} className="inline-flex items-center gap-1 rounded-lg bg-accent px-3 py-2 text-xs font-bold text-[#1a1817]"><CheckCircle2 size={14}/>{busy === order.id ? "Saving…" : "Confirm & collect deposit"}</button> : order.status === "PICKED_UP" ? <button disabled={busy === order.id} onClick={() => action(order, "return")} className="inline-flex items-center gap-1 rounded-lg border border-border px-3 py-2 text-xs font-bold text-text hover:border-accent"><RotateCcw size={14}/>{busy === order.id ? "Saving…" : "Mark returned"}</button> : <button disabled={busy === order.id} onClick={() => action(order, "pickup")} className="inline-flex items-center gap-1 rounded-lg bg-accent px-3 py-2 text-xs font-bold text-[#1a1817]"><Truck size={14}/>{busy === order.id ? "Saving…" : "Mark picked up"}</button>}</td></tr>)}</tbody></table></div><div className="border-t border-border px-5 py-3 text-right"><span className="inline-flex items-center gap-1 text-xs text-chalk"><CheckCircle2 size={14} className="text-green-500"/>Live API data</span></div></Panel>;
}
